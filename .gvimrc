set guifont=Menlo\ Regular:h12
set background=light
highlight ColorColumn guibg=LightGray

" Add a number gutter wide enough that it probably won't get bigger.
set number
set nuw=5

" Set the width to 80+gutter.
set columns=85
